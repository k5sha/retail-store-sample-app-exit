# Token with repo scope (or use GitHub App installation token).
# Must have access to THIS repo so it can post comments on any contributor's PR.
GITHUB_TOKEN=
N8N_HOST=http://localhost:5678
WEBHOOK_URL=http://localhost:5678/webhook/github-pr-review